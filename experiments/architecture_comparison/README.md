# Architecture comparison

The frozen two-episode matched diagnostic is in `results/architecture_diagnostic.csv`. It checks executable integration under identical episodes; it is not a population-level superiority test.
